---
description: Notes - Teach me something durable — a preference, a correction, a fact worth keeping
argument-hint: "[optional: the specific thing to remember]"
---

Save a lasting lesson from this session: $ARGUMENTS

This is what stops you having to repeat yourself. Without it, every correction
lasts exactly one conversation.

1. If something specific was given above, that is the lesson. If not, look back
   over this session for what would genuinely change how I work next time:
   a preference you stated, a correction you made, a constraint I did not know,
   or an approach you confirmed was right.
2. Check `CLAUDE.md` and any existing notes first, so an established rule is
   updated in place rather than duplicated in a second, slightly different form.
3. Show me the exact wording before writing anything. A memory that is vague
   ("be careful with dates") is worse than none; it should be specific enough to
   act on ("meeting times in these notes are local time, never UTC").
4. Once approved, append it to `CLAUDE.md` under a clear heading, with one line
   on **why** — a rule whose reason is lost gets dropped the first time it is
   inconvenient.

Do not save: anything already obvious from the files, anything true only of this
one conversation, or anything you have not confirmed with me.
